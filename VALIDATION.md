# MVP検証結果

2026-09-19実行。Linux/WSL Python 3.10.12、Windows Python 3.10.11 64bit、Chromium + agent-browser、GeneralUser GSで検証。

## 自動テスト

音源を指定して `python -m pytest tests -q`：**48 passed**（最終更新。追加検証は末尾参照）。

テンポ変更をまたぐ音符、複数トラック、velocity=0、同音重複、欠けたNote Off、Type 0、拍子、88鍵の白鍵52/黒鍵36、点灯時刻の画像ピクセル、不正入力、音声発生時刻、トラックミュート、ジョブ競合、動画キャンセル、一時ファイル削除、再試行、Rangeダウンロードを検証。

## ブラウザ

サンプル読み込み、SoundFont WAV読み込み、音声再生の進行、一時停止、4.5秒へのシーク、90 BPM表示、3/4表示、1.5倍速、実効135 BPM、チャンネル別4色、鍵域自動調整、ミュート時の旧音声破棄、音声再合成、設定リセットの14項目を確認。

加えてドラッグ＆ドロップによるType 0読み込み、既存49トラックMIDIのファイル選択読み込み（33演奏トラック表示）、UIからのMP4生成・保存リンクを確認。未処理JavaScriptエラーなし。

ブラウザの音声制限は実クリックで解除して検証。MIDI読み込み完了を待つよう自動テストを修正し、アプリ側も読み込み中は操作をロックした。

## 既存MIDI

元ファイルは読み取りのみ。ソースZIPには含めていません。

| MIDI | 全トラック | ノート | tempoイベント | 秒数 |
|---|---:|---:|---:|---:|
| Four_Piece_Game_Medley.mid | 5 | 24,903 | 16 | 968.864963 |
| Live_Medley_v2.mid | 5 | 21,577 | 22 | 958.462194 |
| UN_Owen_Meta_Knight_Gourmet_Race_MEGALOVANIA.mid | 49 | 23,467 | 43 | 951.704880 |

3本ともMidoの再生時間との差は1e-7秒未満。拍子イベント数は20 / 3 / 10。最後のMIDIにはSysExがあり、非対応の警告を表示。

Four_Pieceは全曲をSoundFont合成（余韻込み970.864966秒）。PCMピーク3150、RMS485.4、クリッピング0。239.096916〜249.096916秒の区間を60fps・1.5倍速で動画化し、155→164→174 BPMの変更を確認。

既存の約16分MIDIはフル尺動画を実行していません。全曲解析・全曲音声合成・テンポ変更区間の動画で確認。

## MP4

すべて1920×1080、H.264/yuv420p、AACステレオ。全編デコード成功。音声RMSは非ゼロ。

| ファイル | fps | フレーム | 映像秒数 | 音声秒数 |
|---|---:|---:|---:|---:|
| Aurora_Study_1080p30.mp4 | 30 | 324 | 10.800000 | 10.799002 |
| Aurora_Study_1080p60.mp4 | 60 | 648 | 10.800000 | 10.799002 |
| Medley_Tempo_Check_1080p60.mp4 | 60 | 400 | 6.666667 | 6.666009 |
| Windows_Smoke_1080p30.mp4 | 30 | 90 | 3.000000 | 3.000000 |

オリジナルサンプルは5トラック・73音、120→90→150 BPM、4/4→3/4→2/4。30/60fpsで全編生成。30fpsはUIから実行。

WindowsネイティブFFmpegでも生成成功。Windows WaitressへのHTTPでMIDI読み込み→SF2→60fps/1秒動画生成→ダウンロードにも成功（97,896 bytes）。

実スピーカーでの聴取評価・全GM音色や全CCの聴き比べは未実施。合成波形・再生状態・同期・MP4ストリームを検証しました。制限はREADME参照。

## 表示と音声の独立切り替え（追加検証）

- `hiddenTracks` を映像専用、`mutedTracks` を音声専用に分離。UI・プレビュー・MP4・CLIに反映。
- 統合テストを追加し、全17件通過。非表示でも音声が同じこと、ミュートしてもノート表示が残ること、表示変更で音声キャッシュを再利用できることを確認。
- ブラウザで10項目通過：各トラックの2スイッチ、音声準備・再生、伴奏を非表示にしたまま再生継続、音声ファイルの維持、全パート発音設定の維持、ミュートしても表示・描画画像が変わらないこと、ミュート時だけ音声を再準備、解除時のキャッシュ再利用。
- Windowsの8766番サーバーを新版へ更新。HTTPで独立設定と、非表示変更後も全楽器の音声キャッシュを使うことを確認。
- 既存Four_Piece_Game_Medleyの冒頭6秒から `Piano_With_Band_1080p60.mp4` を生成。映像はPianoトラックのみ、音声は元の全楽器ミックス。1920×1080 / 60fps、H.264とAAC、音声・映像とも6秒。全編デコード成功。
- 完成画面のスクリーンショットで、ピアノ相当の1トラックだけが表示オン、全4トラックが音声オンであることを目視確認。

## 検索・楽譜・コード・楽器ミキサー（追加検証）

- 最終自動テスト **38 passed**（GeneralUser GS指定）。検索文字のURLエンコード、6サイト、直接URL制限、転送先の再検証、取得サイズ上限、元MIDI保存、エラー後の既存ファイル維持を追加検証。
- ノートのtickとテンポ積分時刻、小節・拍子変更、シーク後の譜面カーソル、パート選択、PNG生成、表示オン/オフ、コード三和音・転回形・7th・判定困難・ドラム除外、Type 0の複数パート、タイを含む小節またぎを確認。
- 共有チャンネルの2トラックで個別ミックスを検証。片方を音量0%にした波形はそのトラックをミュートした波形と一致。Volume / Expressionイベントを含む50%設定のRMS比は0.5±0.015以内。音色変更で波形が変わり、全パート0%で無音。
- ブラウザで検索語付き6リンク、パート切り替え、4.7秒のシークと3/4の楽譜、楽譜・コードの表示切り替え、Violin音色・50%音量での音声合成、UIからのMP4完成リンクを確認。
- Windowsの8766番サーバーを最終版に更新。BitMidiの「Beethoven-Moonlight-Sonata.mid」を曲ページURLから取り込み、1,145ノート・69小節の解析、元MIDIダウンロード、楽譜PNG生成を確認。ダウンロードした第三者MIDIは配布ZIPに含めていません。
- Linuxブラウザの曲ページ取り込みで配布サイトのHTTP 502も発生。アプリはエラーとファイル取り込みの代替方法を表示。続くMIDI本体URLの取り込みは成功。配布サイトの応答やURL構造は将来変わる可能性があります。
- GeneralUser GSでは13種類のドラムキットを列挙。WindowsでBrushキット・Violin音色・パート音量50%/70%を設定した動画を生成。

| 新規動画 | 実行経路 | fps | フレーム | 映像秒数 | 音声秒数 | デコード |
|---|---|---:|---:|---:|---:|---|
| Score_Chords_Mixer_1080p30.mp4 | ブラウザUI → Linux | 30 | 324 | 10.800000 | 10.799002 | 全編成功 |
| Score_Chords_Mixer_1080p60.mp4 | Windows HTTP → Windows FFmpeg | 60 | 648 | 10.800000 | 10.800000 | 全編成功 |

両方とも1920×1080、H.264/yuv420p、AACステレオ。音声RMSは750.48 / 665.31。楽譜と推定Cmaj7が表示された4.7秒の書き出しフレームを目視確認。

既存の大規模MIDIにも追加機能を実行：Four_Piece（24,903音・593小節）、Live_Medley_v2（21,577音・643小節）、49トラック版（23,467音・33演奏パート・608小節）。全曲コード解析と先頭・中間・最終の楽譜描画、および中間フレーム生成が成功。自動選択はそれぞれピアノの `3:2` / `3:2` / `5:1`。今回の更新ではこれらのフル尺動画を再生成していません。

楽譜は簡易採譜で、原曲の出版譜との一致や複雑な声部分けは検証対象外です。コードは推定結果で、実曲の正解ラベルとの精度評価は未実施です。

## MIDIファイル選択時の404修正

- Windowsの標準起動先8765番に旧版サーバーが残っていた。静的JavaScriptだけが更新され、旧サーバーに存在しない `/api/music/<id>` と `/api/score/<id>/0.png` が404になることを再現。
- 古いプロセスを終了し、8765・8766番を新版に更新。両方にmultipartでMIDIファイルを送信し、73ノート・4パートの読み込みと楽譜解析・PNGの200応答を確認。
- `/api/status` にAPI互換バージョンを追加。画面は対応確認が終わるまで入力を無効化し、非互換なら日本語で再起動を案内。APIエラーはパスとステータスをログへ記録。
- ブラウザのファイル選択から楽譜PNG・4パート表示まで確認。検証用サーバーで旧版応答を再現すると、ファイル選択が無効になり、APIへの通信はstatus取得だけになることを確認。
- 回帰テスト追加後 **39 passed**。今回の変更は起動時の互換性チェックとAPI案内であり、追加のフル尺動画再生成は行っていません。

## 複数パート・複数小節・ピアノ基準の小節コード

- 楽譜は複数パートのチェック選択で別段表示。小節数は1〜8、初期値4。同じ設定と小節座標をプレビュー・Pillow書き出しで使用。
- **48 passed**。4小節ページの境界、5小節の曲の最終ページ、テンポ・拍子変更を含むカーソル、選択順によらない段順、全解除、表示・ミュートとの独立、入力範囲、APIのPNGサイズを検証。
- 譜面の音符が左端の音部記号に重ならず、対応する小節の枠内に描かれることを画像ピクセルで検証。
- ピアノのC→F和音と、強い音量の別楽器B和音を重ねたMIDIで、推定がピアノのC→Fのみになることを確認。小節途中でコードが切り替わらず、楽譜選択にも影響されないことを確認。
- ブラウザで全選択・全解除・2パート選択、1/2/4/8小節への変更、8.1秒へのシークによる5小節目のページ表示、逆方向のシークを確認。小節数4、2パートのPNG高さ560。未処理JavaScriptエラーなし。
- サンプルのコード開始時刻は0、2、4、6.000001、8.000002秒で、小節頭と一致。元MIDIでピアノ音色の1パートだけを解析対象として確認。
- Windowsの8765・8766番の両方でAPI互換バージョン3、2パート・4小節、先頭と5小節目のPNG取得を確認。
- Windowsで `Multi_Part_Score_4_Bars_1080p60.mp4` を生成。1920×1080・60fps・648フレーム・10.8秒、H.264/yuv420pとAACステレオ。全編デコード成功、音声RMS755.14。8.1秒のフレームで2段とも5小節目へ切り替わっていることを確認。
- 既存 `Four_Piece_Game_Medley.mid`（24,903音）でもGuitar/Bass/Pianoの3段・4小節表示を生成。149ページ、コード解析対象はPianoの `3:2` のみ。239.5秒のシークでは149〜152小節のページが選ばれ、現在小節は152。


## v10 realtime audio queue fix
- Realtime stem media elements use `preload=metadata` and wait only for metadata during preparation.
- Bulk stem synthesis concurrency is limited to 2.
- Waitress worker threads are increased to 16.
- A 3-minute per-stem watchdog turns a true synth hang into a visible error instead of an infinite `N/M` state.
